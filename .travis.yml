language: objective-c
osx_image: beta-xcode6.3

branches:
  only:
    - master

before_install:
  - gem install xcpretty

before_script:
  - set -o pipefail

script:
  - xcodebuild build test -project TBCCore.xcodeproj -scheme TBCCore-mac.framework -sdk macosx | xcpretty
